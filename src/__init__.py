"""
src package

Main source code for the Catholic Mass Times Scraper.

Sub-packages:
    scrapers/  — Code that fetches web pages (HTTP requests)
    parsers/   — Code that extracts data from HTML (BeautifulSoup parsing)
    utils/     — Shared utility functions (logging setup, file I/O, etc.)
"""
