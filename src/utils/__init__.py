"""
utils package

Shared utility functions used across the project.

Modules:
    logger.py   — Sets up logging to console and file
    file_io.py  — Helper functions for reading/writing CSV and JSON files
    http.py     — HTTP request wrapper with retries, rate limiting, and error handling
"""
