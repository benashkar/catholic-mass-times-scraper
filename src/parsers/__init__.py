"""
parsers package

Contains modules that extract structured data from raw HTML pages.

Modules:
    church_parser.py      — Extracts church name, address, contact info
    mass_time_parser.py   — Extracts recurring mass times by day of week
    confession_parser.py  — Extracts confession time ranges
    address_parser.py     — Splits raw address strings into structured fields
"""
